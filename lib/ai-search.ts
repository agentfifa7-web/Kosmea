// Cet environnement ne permet pas de supprimer des fichiers : ce module est
// conservé pour compatibilité mais n'est plus utilisé. Voir lib/assistant.ts
// (moteur KÔSMÉA AI basé sur le catalogue beauté & mode).
export {
  parseQuery,
  matchProducts,
  matchLooks,
  buildLookProposals,
  describeQuery,
  assistantSuggestions,
  type ParsedQuery,
  type LookProposal,
} from './assistant'
